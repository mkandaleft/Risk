
#include "../include/LoggingObserver.h"


void testLoggingObserver() {

    //std::cout << "Starting Testing LoggingObserver" << std::endl;

    // Create a new LogObserver
    //LogObserver* logObserver = new LogObserver();
/*
    //Create a new Order and execute
    Orders* order = new Orders("Test Order");
    order->attach(logObserver);
    order->execute();
    
    // Create a new OrderList and add the order to it
    OrdersList* orderList = new OrdersList();
    orderList->attach(logObserver);
    orderList->addOrder(*order);

    // Create a new Command and save effect
    Command* command = new Command("Test Command");
    command->attach(logObserver);
    command->saveEffect("Test Effect");

    // Create a new CommandProcessor and save the command
    CommandProcessor* commandProcessor = new CommandProcessor();
    commandProcessor->attach(logObserver);
    commandProcessor->saveCommand(command);

    // Create a new GameEngine and transition state
    //GameEngine* gameEngine = new GameEngine("Test State");
    //gameEngine->attach(logObserver);
    //gameEngine->setState("Test State 2");

    std::cout << "Finished Testing LoggingObserver" << std::endl;
*/
}

//int main() {
//    testLoggingObserver();
//    return 0;
//}

// g++ src/LoggingObserverDriver.cpp src/LoggingObserver.cpp src/Orders.cpp src/CommandProcessing.cpp  -o mainDriver2